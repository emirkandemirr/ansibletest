---
- name: Create Folder on Local Machine
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Create Folder
      file:
        path: /home/emirkandemir/playbooktest.txt
        state: touch
